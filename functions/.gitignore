node_modules/
*.local

# Environment files (Cloud Functions secrets go via
# `firebase functions:secrets:set`, never in .env)
.env
.env.*
!.env.example

# Firebase CLI debug logs
firebase-debug.log
firebase-debug.*.log

# Build / pack outputs
*.tgz

# Service account keys (defense-in-depth alongside root .gitignore)
*-firebase-adminsdk-*.json
service-account-*.json