module.exports = {
  root: true,
  env: { browser: true, es2020: true },
  extends: [
    'eslint:recommended',
    'plugin:react/recommended',
    'plugin:react/jsx-runtime',
    'plugin:react-hooks/recommended',
  ],
  ignorePatterns: ['dist', '.eslintrc.cjs'],
  parserOptions: { ecmaVersion: 'latest', sourceType: 'module' },
  settings: { react: { version: '18.2' } },
  plugins: ['react-refresh'],
  rules: {
    'react/jsx-no-target-blank': 'off',
    // PropTypes is the React 15-era runtime-validation pattern; the
    // modern React 18 + Vite codebase relies on TypeScript or convention
    // rather than declaring propTypes on every component. Off project-wide
    // so contributors can write idiomatic functional components without
    // every destructured-prop component tripping a warning that the
    // existing codebase intentionally ignores.
    'react/prop-types': 'off',
    'react-refresh/only-export-components': [
      'warn',
      { allowConstantExport: true },
    ],
  },
}
