# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.env

# Python (Metadata Generation System / scripts/)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Firebase service account keys (any project)
llm-hyper-audio-firebase-adminsdk-fbsvc-*.json
civil-rights-history-project-firebase-adminsdk-*.json
*-firebase-adminsdk-*.json
service-account-*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

run_sample.log
run_sample_output.json
.pa11yci-report.json

# Per-user Claude Code state (settings, scheduled-task lockfiles, etc.)
.claude/

# Phase 3 derived artifacts — transcripts/corrected/ is regenerated by
# running scripts/apply_corrections.py against transcripts/raw/ + the
# CLEANED_TRANSCRIPTS_REVIEW.md overlay. Not committed because (a) it's
# fully reproducible from the inputs, (b) it would inflate the repo with
# duplicate text content. Keep raw/ committed (the canonical input);
# regenerate corrected/ on demand.
transcripts/corrected/

# rag/ env-var file — copied from rag/.env.example and populated with
# real credentials. Never commit (Pinecone + Voyage API keys).
rag/.env.local
rag/.env
